var should = require('chai').should(),
    _ = require('lodash');

/* Chai example

describe('Test Module', function () {

	var Globalvars;

    before(function (done) {
    	Globalvars = require('../src/js/model.js');
    	done();
    });

    after(function () {});

    it('init test', function () {
		Globalvars.should.have.property('method');
    });

});

*/